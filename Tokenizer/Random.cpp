#include <iostream>
/*C-style Comment*/
void Random::Function()
{
	char C = '"';
	std::string Demo = "Project 1";
	std::cout << Demo;
	Demo += "Lexical Scanner";
	// It is a CPP comment
	for (int i = 0; i < 5; ++i)
		int num = nextInt(20);
	return rand() % range;
	for (int i = 0; i < N; ++i)
	{
		std::string DoNothing;
	}
}