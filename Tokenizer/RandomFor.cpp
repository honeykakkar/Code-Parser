void Random::Function()
{
	for (int i = 0; i < 5; ++i)
		int num = nextInt(20);
	return rand() % range;
	for (;;);
	for (;;)
		std::string TestPassed;

	for (int i = 0; i < N; ++i)
	{
		std::string DoNothing;
	}

	for ( ; ; );
	for (; ; )
		std::string KeepLooping;
}